const Header = () => {
    return (
        <header className="header">
            <h1>Tanxy's Bot Store</h1>
            <button className="signup-btn">Sign Up</button>
        </header>
    )
}

export default Header
